import styled from "@emotion/styled"

export const Main = styled.main`
  margin-left: auto;
  margin-right: auto;
  max-width: ${({ theme }) => theme.breakpoints.md};
  padding-top: 40px;
  padding-left: 30px;
  padding-right: 30px;
`
